Pasang Script ini di DHCP Server tab Screipt

:local queueName "Siswa - $leaseActMAC";

:if ($leaseBound = "1") do={
    /queue simple add name=$queueName target=($leaseActIP . "/32") limit-at=4M/4M max-limit=6M/6M parent=INTERNET-SISWA disabled=no comment=[/ip dhcp-server lease get [find where active-mac-address=$leaseActMAC && active-address=$leaseActIP] host-name];
} else={
    /queue simple remove $queueName
}
